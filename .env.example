# Example environment variables only.
# Never place real credentials in this file.
# Copy this file to .env locally when an application or script requires it.
# The real .env file must remain private and is ignored by .gitignore.

CLOUDFLARE_API_TOKEN=replace_with_your_api_token
CLOUDFLARE_ACCOUNT_ID=replace_with_your_account_id
CLOUDFLARE_ZONE_ID=replace_with_your_zone_id
